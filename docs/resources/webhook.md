---
# generated by https://github.com/hashicorp/terraform-plugin-docs
page_title: "devhelm_webhook Resource - DevHelm"
subcategory: ""
description: |-
  Manages a DevHelm outbound webhook endpoint that receives event payloads.
---

# devhelm_webhook (Resource)

Manages a DevHelm outbound webhook endpoint that receives event payloads.

## Example Usage

```terraform
# Outbound webhook for forwarding incident events into your own systems.
resource "devhelm_webhook" "audit_sink" {
  url               = "https://audit.internal.example.com/devhelm/events"
  description       = "Mirrors all incident lifecycle events into the audit log."
  enabled           = true
  subscribed_events = ["incident.opened", "incident.resolved", "incident.acknowledged"]
}
```

<!-- schema generated by tfplugindocs -->
## Schema

### Required

- `subscribed_events` (Set of String) Set of event types this webhook subscribes to
- `url` (String) Webhook URL that receives event payloads

### Optional

- `description` (String) Human-readable description of this webhook. Note: the API currently treats null as 'preserve current' and does NOT support clearing the description once set (unlike status_page descriptions). Removing this attribute from HCL after a value has been set will leave the existing description on the server unchanged. Track API parity here: https://devhelm.io/docs/api/webhook-description-clear.
- `enabled` (Boolean) Whether the webhook is enabled (default: true)

### Read-Only

- `id` (String) Unique identifier

## Import

Import is supported using the following syntax:

The [`terraform import` command](https://developer.hashicorp.com/terraform/cli/commands/import) can be used, for example:

```shell
terraform import devhelm_webhook.audit_sink 4d5e6f70-1234-5678-9abc-def012345678
```
